# Subset de dependencias APENAS para o deploy no Streamlit Cloud, que nao
# suporta Poetry. A fonte de verdade das dependencias e pyproject.toml +
# poetry.lock; este arquivo cobre so o necessario para a demo do frontend
# (sem fastapi, mlflow, dvc, redis). Ao mudar versoes no pyproject, revise aqui.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cpu
scikit-learn>=1.5.0
pandas>=2.2.0
numpy>=1.26.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
faiss-cpu>=1.8.0
streamlit>=1.36.0
plotly>=6.9.0
